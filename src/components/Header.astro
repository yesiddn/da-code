---
import NavBar from "./NavBar.astro";

interface Props {
  pageTitle: string;
}

const { pageTitle } = Astro.props;
---

<header class="fixed flex justify-around w-full my-8">
  <img src="img/logo.svg" alt="DaCode logo">

  <NavBar pageTitle={pageTitle} />
</header>
